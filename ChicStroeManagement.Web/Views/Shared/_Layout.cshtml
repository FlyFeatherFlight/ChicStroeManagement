<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - CHIC店面管理平台</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <script src="~/Scripts/jquery-3.3.1.js"></script>
    
    <script src="~/Scripts/jquery.cookie.js"></script>
    @*<script language="javascript" type="text/javascript">
        $(function () {
   
    var getCookie = function (name) {
      var arr;
      var reg = new RegExp("(^| )" + name + "=([^;]*)(;|$)");
      if (arr = document.cookie.match(reg))
        return unescape(arr[2]);
      else
        return null;
    };
            //$('#login').hide();
            if (getCookie(".userinfo")) {

            
                //$('#signUp').hide();
                $('#logOut').show();
            }
            else {
                $('#logOut').hide();

                //$('#signUp').show();
            }
        })
    </script>*@
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top" id="fir">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("CHIC CASA", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse" id="two">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("主页", "Index", "Home")</li>
                    <li>@Html.ActionLink("关于", "About", "Home")</li>
                    <li>@Html.ActionLink("联系方式", "Contact", "Home")</li>
                </ul>

                <ul class="nav navbar-nav right">

                    @*<li id="login">@Html.ActionLink("登录", "SignIn", "Account")</li>*@
                    @*<li id="signUp">@Html.ActionLink("注册", "SignUp", "Account")</li>*@

                    <li id="logOut">@Html.ActionLink("注销", "SignOut", "LogIn")</li>
                </ul>

            </div>
        </div>
     
    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year -CHIC店面管理系统 by至尚至美信息部</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
